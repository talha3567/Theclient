package com.pulseclient.modules.render;

import com.pulseclient.Category;
import com.pulseclient.Module;

public class PulseBetterTooltips extends Module {
    public PulseBetterTooltips() {
        super("PulseBetterTooltips", "Ported from Meteor", Category.RENDER);
    }
}
